import React from "react"
import { HeaderStyle, HeaderTitle } from "../style/styled"

const Header: React.FC = () => {
  return (
    <HeaderStyle>
      <HeaderTitle>Шагомер на тестовое задание</HeaderTitle>
    </HeaderStyle>
  )
}

export default Header
